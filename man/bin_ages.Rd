% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bin-ages.R
\name{bin_ages}
\alias{bin_ages}
\title{Make age bins}
\usage{
bin_ages(x, breaks)
}
\arguments{
\item{x}{A numeric vector of ages.}

\item{breaks}{A numeric vector determining the lower bounds for age bins.
\code{"lifex5"} or \code{"lifex10"} returns age bins specific to life expectancy
calculations consisting mainly of either 5- or 10-year intervals.}
}
\value{
A factor vector the same length as \code{x}.
}
\description{
Input ages and return age bins.
}
\examples{
ages <- seq(.5, 10, .5)

# 5-year bins
data.frame(
  age = ages,
  bin = bin_ages(ages, breaks = seq(0, 10, 5))
)

# Variable size bins
data.frame(
  age = ages,
  bin = bin_ages(ages, breaks = c(0, 3, 4, 5, 9))
)

# 10-year bins for life expectancy calculations
data.frame(
  age = 0:100,
  bin = bin_ages(0:100, breaks = "lifex10")
)

}
